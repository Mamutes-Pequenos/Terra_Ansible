apiVersion: v1
kind: Service
metadata:
  name: grafana-service
spec:
  selector:
    app: grafana
  ports:
    - protocol: TCP
      port: 3000  # Porta externa para acesso
      targetPort: 3000  # Porta do container
  type: LoadBalancer  # Expõe o serviço externamente (se necessário)
